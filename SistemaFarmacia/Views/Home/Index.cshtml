
<div class="dashboard">
    @if (Context.Session.GetString("Usuario") != null)
    {
        <h4>Bienvenido @Context.Session.GetString("Usuario")</h4>
    }
    <h1>Sistema Farmacia</h1>

    <p>Gestiona ventas, productos y categorías desde el menú superior.</p>

    <a asp-controller="Ventas" asp-action="NuevaVenta" class="btn btn-verde">
        Nueva Venta
    </a>
</div>